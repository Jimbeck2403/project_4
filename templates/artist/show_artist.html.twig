{% extends 'base.html.twig' %}

{% block body %}
    <article>
        <h2>{{ artist.name}}</h2>
        <div class="content">
            <img src="{{ artist.image }}" alt="">
            {{ artist.description }}
        </div>
    </article>
{% endblock %}