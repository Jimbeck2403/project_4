{% extends 'base.html.twig' %}

{% block body %}
    <div class="card mb-3">
        <h3 class="card-header">{{ performance.name }}</h3>
        <img style="height: 300px; width: 100%; display: block;" src="{{ performance.image }}" alt="Card image">
        <div class="card-body">
            {{ performance.description }}
        <div class="card-body">
    </div>
{% endblock %}